{
  "name": "day-6",
  "version": "1.0.0",
  "main": "build/index.js",
  "license": "MIT",
  "scripts": {
    "build": "babel src/ -d build/",
    "prestart": "yarn run build",
    "start": "node ."
  },
  "dependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.7.0",
    "babel-preset-stage-0": "^6.24.1"
  },
  "babel": {
    "presets": [
      "env",
      "stage-0"
    ]
  }
}
